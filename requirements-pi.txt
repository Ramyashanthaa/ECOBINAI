# Raspberry Pi 5 / ARM64 hardware dependencies
# Installs the core EcoBinAI deps plus the PCA9685 + GPIO + servo stack.
-r requirements.txt

# Direct GPIO PWM path (legacy `hardware_driver=gpio` mode)
RPi.GPIO==0.7.1
gpiozero==1.7.0

# Adafruit Blinka + PCA9685 — required for `hardware_driver=pca9685`.
# These drive 4 SG90 servos via I2C from the Pi 5's pins 3 (SDA) / 5 (SCL).
adafruit-blinka>=8.0
adafruit-circuitpython-busdevice>=5.0
adafruit-circuitpython-pca9685>=3.4
adafruit-circuitpython-motor>=3.4
